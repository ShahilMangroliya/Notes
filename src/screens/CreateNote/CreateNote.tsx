import React from 'react';
import SafeAreaContainer from '@/components/SafeAreaContainer';
import StyledText from '@/components/Text';

const CreateNote = () => {
  return (
    <SafeAreaContainer>
      <StyledText>CreateNote</StyledText>
    </SafeAreaContainer>
  );
};

export default CreateNote;
